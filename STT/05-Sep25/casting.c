#include <stdio.h>
int main() {
  int a = 2;
  int b = 5;
  int c;
  int x;
  c = b / a;
  double A = 2;
  double B = 5;
  double C;
  C = B / A;
  x = C;
  C = b / a;
  C = b / (double)a;

  return 0;
}